# Vercel Environment Variables
# Set these in your Vercel dashboard under Settings > Environment Variables

# Your local server URLs (replace with your actual local IP if needed)
NEXT_PUBLIC_AUTH_URL=http://localhost:3001
NEXT_PUBLIC_GAME_URL=http://localhost:3002

# Or if you want to use your local IP address for external access:
# NEXT_PUBLIC_AUTH_URL=http://192.168.1.100:3001
# NEXT_PUBLIC_GAME_URL=http://192.168.1.100:3002

# Supabase (same as local)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key